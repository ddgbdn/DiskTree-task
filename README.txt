Practice task from Ulearn course [Data Structures]

Input: A list of strings, corresponding a full directory pathes. 
Every path do not contains any spaces and do not exceed 80 symbols,
Contained in the list once and consist of directory names divided by "\".

Output: A list of strings, corresponding to formated catalog tree.
In front of every catalog name, there should be spaces, which correspond to how deeply it's nested.
Subcatalogs must be listed after catalog's name in lexicographical order and their names must precede one space more, than name of parent catalog.
Top-Level catalogs should not be preceded by any spaces and	should be listed in lexicographical order.

Investigate tests to better understand output format.